# Front-End (Vite/React) Environment Variables
# The secure, compiled Cloudflare Worker URL (e.g., https://drsky-worker.yourname.workers.dev)
VITE_WORKER_URL=

# Back-End (Cloudflare Worker) Environment Variables
# The secure cryptography key used to sign and verify admin login session JWTs
JWT_SECRET=
